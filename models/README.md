# RequantAi local models

Model weights stay out of Git. The dispatcher works without a model through deterministic routing and rules. Approved GGUF entries in `registry.json` may be served by llama.cpp when present; promotion always requires evaluation and operator approval.
