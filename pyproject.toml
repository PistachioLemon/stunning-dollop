[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "requantai-dispatcher"
version = "3.0.0-pc"
description = "CPU-first RequantAi Dispatcher and AI Hauling edge runtime"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
