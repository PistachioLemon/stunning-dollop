# Security

Keep configuration, credentials, runtime databases, model weights, vehicle identifiers, operator data, proprietary mechanisms, and patent materials out of Git. Bind the sandbox to loopback. Vehicle-bus writes, bookings, payments, signatures, HOS changes, safety overrides, model promotion, and repair execution require explicit authorization and an audit record.
